class: Payment
table: payments
fields:
  amount: int # Amount in pence (GBP)
  date: DateTime
  reference: String
  method: PaymentMethod
  payee: String
  bankAccount: BankAccount?, relation(optional)
  parent: Parent?, relation(optional) # might not have classified it yet