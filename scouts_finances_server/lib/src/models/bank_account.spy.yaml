class: BankAccount
table: bank_accounts
fields:
  accountNumber: String
  sortCode: String
  name: String # Name of the account holder
  parent: Parent?, relation(optional, name=parent_bank_acc) # Association with a parent